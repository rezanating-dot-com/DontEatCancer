services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: donteatcancer
      POSTGRES_PASSWORD: donteatcancer
      POSTGRES_DB: donteatcancer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
